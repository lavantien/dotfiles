# Selene configuration for Neovim Lua files
std = "vim"

exclude = [
    "node_modules/",
    "vendor/",
    ".git/",
]

[lints]
# fzf-lua setup() mixes a profile string with option keys by design
mixed_table = "allow"
